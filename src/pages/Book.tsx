import React from "react";
import Grid from "@material-ui/core/Grid";
import Typography from "@material-ui/core/Typography";

import Page from "./Page";

const Book = () => {
  return (
    <Page>
      <Typography variant="h4">Hello from Book 👋</Typography>
    </Page>
  );
};

export default Book;
